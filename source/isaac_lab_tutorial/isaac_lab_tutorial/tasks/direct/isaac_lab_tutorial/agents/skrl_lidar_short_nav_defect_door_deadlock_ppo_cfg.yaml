seed: 42


# Models are instantiated using skrl's model instantiator utility
# https://skrl.readthedocs.io/en/latest/api/utils/model_instantiators.html
models:
  separate: False
  policy:
    class: GaussianMixin
    clip_actions: True
    clip_log_std: True
    min_log_std: -20.0
    max_log_std: 2.0
    initial_log_std: 0.0
    network:
      - name: lidar_net
        input: "STATES[:, 16:].view(-1, 1, 90, 1)"
        layers:
          - conv2d: [16, [5, 1], [2, 1], 0]
          - conv2d: [32, [3, 1], [2, 1], 0]
          - flatten: {}
          - 256
        activations: [elu, elu, "", elu]
      - name: fuse
        input: "concatenate([lidar_net, STATES[:, :16]])"
        layers: [256, 128]
        activations: elu
    output: ACTIONS
  value:
    class: DeterministicMixin
    clip_actions: False
    network:
      - name: lidar_net
        input: "STATES[:, 16:].view(-1, 1, 90, 1)"
        layers:
          - conv2d: [16, [5, 1], [2, 1], 0]
          - conv2d: [32, [3, 1], [2, 1], 0]
          - flatten: {}
          - 256
        activations: [elu, elu, "", elu]
      - name: fuse
        input: "concatenate([lidar_net, STATES[:, :16]])"
        layers: [256, 128]
        activations: elu
    output: ONE


memory:
  class: RandomMemory
  memory_size: -1


agent:
  class: PPO
  # Match the proven v18a architecture/settings while isolating door_deadlock runs.
  rollouts: 256
  learning_epochs: 8
  mini_batches: 16
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 3.0e-05
  learning_rate_scheduler: KLAdaptiveLR
  learning_rate_scheduler_kwargs:
    kl_threshold: 0.01
  state_preprocessor: RunningStandardScaler
  state_preprocessor_kwargs: null
  value_preprocessor: RunningStandardScaler
  value_preprocessor_kwargs: null
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.12
  value_clip: 0.2
  clip_predicted_values: True
  entropy_loss_scale: 0.003
  value_loss_scale: 0.3
  kl_threshold: 0.0
  rewards_shaper_scale: 1.0
  time_limit_bootstrap: False
  experiment:
    directory: "lidar_short_nav_defect_door_deadlock_direct"
    experiment_name: ""
    write_interval: auto
    checkpoint_interval: 3200


trainer:
  class: SequentialTrainer
  timesteps: 10000000
  environment_info: log
